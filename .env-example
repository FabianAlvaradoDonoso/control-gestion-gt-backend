# Entorno de la aplicación (development | production | test)
NODE_ENV=development

# Puerto del servidor
PORT=8000

# URL de la API 
API_URL=

# === Base de Datos PostgreSQL ===
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=control_gestion

# === JWT Configuration ===
# Clave secreta para firmar tokens JWT (REQUERIDA)
# Genera una clave segura con: openssl rand -base64 32
SECRET_KEY=your-super-secret-key-here-change-me

# Algoritmo de encriptación JWT
ALGORITHM=HS256

# === APIs Externas ===
# API Key para BUK 
BUK_API_KEY=

# === CORS Configuration ===
# Lista de orígenes permitidos en producción (separados por coma)
# Ejemplo: https://app.example.com,https://admin.example.com
# Usa '*' para permitir todos (solo desarrollo, no recomendado en producción)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
